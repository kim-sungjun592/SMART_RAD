# 2026-07-14 — 백엔드 전면 재구축 · 신규 API · 8메뉴 프론트 재구성 · ERD

## 요약
- **범위**: 백엔드 도메인 v3 재구축 + 복지/수당/휴가정책/대시보드 API + 프론트 8메뉴 재구성 + 랜딩 개선 + ERD 문서
- **담당**: 김재아, 부현민, 송현빈(팀장)

---

### 1. 백엔드 도메인 재구축 (v3) — 김재아
- 엔티티 PK/컬럼 정비(`<table>_id` 체계) 및 도메인 리팩터: `appointment`, `attendance`, `employee`, `department`, `employmenttype`
- 증명서(`certificateissue`) 도메인 제거, 휴가(`leave`) 도메인에 `LeaveType` 도입 및 `LeaveRequest`/`LeaveBalance` 재구성

### 2. 신규 백엔드 API — 김재아
- **복지/수당**: `allowance`(Allowance/EmployeeAllowance 엔티티·서비스·컨트롤러)
- **휴가 정책**: `leave/LeavePolicy`, `LeaveType` 컨트롤러/DTO
- **대시보드**: `dashboard`(정원·근태 요약 DTO·서비스·컨트롤러)

### 3. 프론트 8메뉴 재구성 — 김재아
- 신규 페이지: `appointments`, `attendance`(+`monthly`), `dashboard/attendance`, `dashboard/headcount`, `payroll`(+`allowance`, `settlement`), `leaves/policy`
- 정리: 기존 `salary`·`records/*`·`dashboard/page` 페이지 제거, `(dashboard)/layout` 갱신, API 매핑 재정비

### 4. 랜딩 UI/UX · 버그 수정 — 부현민
- 랜딩 페이지 UI/UX 개선 + `frontend-design` 폴더 추가
- 백엔드 중복 빈(bean) 충돌, 도커 lockfile 불일치 수정, HR 대시보드 CSS 복구 (`application.properties`, `frontend/Dockerfile`, `LeaveService` 등)

### 5. ERD 문서 추가 — 송현빈(팀장)
- 인사관리 ERD/명세 문서 추가 (`인사관리_Smart_RAD.md`, 약 650줄)

### 변경 파일 (대표)
| 영역 | 파일 |
| --- | --- |
| 신규 API | `allowance/**`, `dashboard/**`, `leave/{controller,dto,entity}/LeavePolicy·LeaveType*` |
| 도메인 재구축 | `appointment/**`, `attendance/**`, `employee/Employee.java`, `leave/**` |
| 프론트 재구성 | `(dashboard)/{appointments,attendance,dashboard,payroll,leaves}/**`, `(dashboard)/layout.tsx` |
| 랜딩/디자인 | `components/landing/**`, `frontend-design/` |
| ERD | `인사관리_Smart_RAD.md` |

## 담당
- 김재아, 부현민, 송현빈(팀장)

## 근거 커밋
- `MJB9595`(김재아): db reworked and basic front added / backend reworked v3 / feat: Add backend APIs for Welfare, Allowance, LeavePolicy, and Dashboard / feat: Restructure 8-menu frontend pages and API mapping
- `hyunminboo`(부현민): Update landing page UI/UX and add frontend-design folder / Fix backend duplicate bean, docker lockfile mismatch, and restore HR dashboard CSS
- `bingak36`(송현빈): erd추가

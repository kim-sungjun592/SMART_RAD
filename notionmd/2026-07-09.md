# 2026-07-09 — 프로젝트 초기 세팅 (백엔드 스캐폴딩 · 랜딩)

## 요약
- **범위**: 저장소 초기화 + 백엔드 전 도메인 뼈대 + 인프라(도커/보안) + 랜딩 초안
- **담당**: 송현빈(팀장), 김재아

---

### 1. 저장소 · 문서 초기화 — 송현빈(팀장)
- `.gitignore`, `README.md` 등 저장소 기본 구성 및 초기 커밋

### 2. 백엔드 도메인 스캐폴딩 — 김재아
- 핵심 도메인 엔티티/컨트롤러/서비스/DTO 일괄 생성: `employee`, `department`, `position`, `employmenttype`, `attendance`(근태), `appointment`(발령), `eventsupport`(경조비), `certificateissue`(증명서)
- 공통 기반: `common`(BaseEntity/DeletableEntity/예외처리/문서번호 생성기), `security`(JWT 인증 필터·토큰 프로바이더·SecurityConfig), `auth`(로그인)
- Flyway 마이그레이션 `V1__core_schema` ~ `V4__attendance_appointment_event_certificate`

### 3. 인프라 — 김재아
- `backend/Dockerfile`, 루트 `docker-compose.yml`(백엔드+DB+프론트), CORS/JPA Auditing 설정

### 4. 프론트 · 랜딩 초안 — 김재아
- Next.js 프로젝트 초기 구성 + 랜딩 페이지 뼈대

### 변경 파일 (대표)
| 영역 | 파일 |
| --- | --- |
| 백엔드 도메인 | `backend/src/main/java/com/tphr/hr/{employee,department,position,attendance,appointment,...}/*` |
| 보안/인증 | `security/JwtAuthenticationFilter.java`, `security/SecurityConfig.java`, `auth/AuthController.java` |
| 마이그레이션 | `db/migration/V1~V4_*.sql` |
| 인프라 | `backend/Dockerfile`, `docker-compose.yml`, `frontend/Dockerfile` |

## 담당
- 송현빈(팀장), 김재아

## 근거 커밋
- `bingak36`(송현빈): Initial commit
- `MJB9595`(김재아): Initial commit for backend / base backend and landing
